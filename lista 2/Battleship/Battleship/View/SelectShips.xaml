<UserControl x:Class="Battleship.View.SelectShips"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Battleship.View"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <UserControl.Resources>
        <Style TargetType="{x:Type StackPanel}">
            <Setter Property="Orientation"
                    Value="Vertical" />
            <Setter Property="Margin"
                    Value="0 20 0 0" />
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <StackPanel.Resources>
            <Style TargetType="Button"
                   BasedOn="{StaticResource ButtonOptions}" />
            <Style TargetType="TextBlock"
                   BasedOn="{StaticResource HeaderTitle}" />
        </StackPanel.Resources>


        <TextBlock Text="Wybieranie statków" />

        <Button Content="Statek z 5 polami" />
        <Button Content="Statek z 4 polami" />
        <Button Content="Statek z 3 polami" />
        <Button Content="Statek z 2 polami" />
        <Button Content="Statek z 1 polem" />
    </StackPanel>
</UserControl>
