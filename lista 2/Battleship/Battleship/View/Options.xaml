<UserControl x:Class="Battleship.View.Options"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Battleship.View"
             xmlns:viewmodel="clr-namespace:Battleship.ViewModel"
             d:DataContext="{d:DesignInstance Type=viewmodel:GameViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450">

    <UserControl.Resources>
        <Style TargetType="{x:Type StackPanel}">
            <Setter Property="Orientation"
                    Value="Vertical" />
            <Setter Property="Margin"
                    Value="0 20 0 0" />
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <StackPanel.Resources>
            <Style TargetType="Button"
                   BasedOn="{StaticResource ButtonOptions}" />
            <Style TargetType="TextBlock"
                   BasedOn="{StaticResource HeaderTitle}" />
        </StackPanel.Resources>

        <TextBlock Text="Opcje" />
        <Button Content="Gotowy"
                Command="{Binding SubmitReadyCommand}" CommandParameter="{Binding .}"/>
        <Button Content="Reset" Command="{Binding RestartGameCommand}"/>
        <Button Content="Rotacja statku" />
    </StackPanel>
</UserControl>
